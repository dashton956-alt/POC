export TESTING=1
export PYTHONUNBUFFERED=1
export OAUTH2_ACTIVE=0
export LOG_LEVEL=DEBUG
export WEBSOCKET_BROADCASTER_URL="redis://localhost:6379"

# Network API Management Configuration
# Copy this to .env and configure your API endpoints

# =============================================================================
# CISCO CATALYST CENTER (DNA CENTER) CONFIGURATION
# =============================================================================
# Cisco Catalyst Center API endpoint
export CATALYST_CENTER_URL=https://catalyst-center.company.com
export CATALYST_CENTER_USERNAME=api-user
export CATALYST_CENTER_PASSWORD=secure-password
export CATALYST_CENTER_CLIENT_ID=client-id
export CATALYST_CENTER_CLIENT_SECRET=client-secret

# =============================================================================
# JUNIPER MIST CLOUD CONFIGURATION  
# =============================================================================
# Mist Cloud API configuration
export MIST_CLOUD_TOKEN=your-mist-api-token
export MIST_ORG_ID=your-organization-id

# =============================================================================
# ARISTA CLOUDVISION PORTAL (CVP) CONFIGURATION
# =============================================================================
# Arista CVP API endpoint
export ARISTA_CVP_URL=https://cvp.company.com
export ARISTA_CVP_USERNAME=cvp-user
export ARISTA_CVP_PASSWORD=cvp-password

# =============================================================================
# JUNIPER SPACE CONFIGURATION
# =============================================================================
# Juniper Space Network Management Platform
export JUNIPER_SPACE_URL=https://space.company.com
export JUNIPER_SPACE_USERNAME=space-user
export JUNIPER_SPACE_PASSWORD=space-password

# =============================================================================
# FORTINET FORTIMANAGER CONFIGURATION
# =============================================================================
# FortiNet FortiManager centralized management
export FORTIMANAGER_URL=https://fortimanager.company.com
export FORTIMANAGER_USERNAME=fortimgr-user
export FORTIMANAGER_PASSWORD=fortimgr-password

# =============================================================================
# PALO ALTO PANORAMA CONFIGURATION
# =============================================================================
# Palo Alto Panorama centralized firewall management
export PANORAMA_URL=https://panorama.company.com
export PANORAMA_API_KEY=your-panorama-api-key

# =============================================================================
# DEFAULT DEVICE CREDENTIALS (BY PLATFORM)
# =============================================================================
# These are used when device-specific credentials aren't available in NetBox

# Cisco IOS/IOS-XE default credentials
export CISCO_DEFAULT_USERNAME=admin
export CISCO_DEFAULT_PASSWORD=admin
export CISCO_ENABLE_PASSWORD=enable-secret

# Cisco NX-OS default credentials
export CISCO_NXOS_USERNAME=admin
export CISCO_NXOS_PASSWORD=admin

# Juniper JunOS default credentials
export JUNIPER_USERNAME=admin
export JUNIPER_PASSWORD=juniper123

# Arista EOS default credentials
export ARISTA_USERNAME=admin
export ARISTA_PASSWORD=arista123
